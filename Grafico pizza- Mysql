Construa um gráfico de pizza no metabase informando a quantidade de pessoas por generos:

Funções utilizadas: Operação Matemática | Agrupamento
Tabelas: leads_basic_details
Query: select gender as Generos, count(gender) as Quantidade from leads_basic_details group by gender
